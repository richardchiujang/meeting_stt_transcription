STT (語音轉文字) 程式使用說明

1.請先選擇 [模型]、[語言模式]、(是/否)勾選 [即時轉錄]

2.[模型] 大小 base < small < medium (越大的速度越慢、效果越好)

3.選擇使用 [錄製麥克風] 或 [選取檔案]

4.[錄製麥克風] 時如非勾選 [即時轉錄]，就是先單純錄音，等到按下 [停止錄音並轉錄] 時開始進行STT

5.欲停止 (中斷) 時請按 [停止轉錄] ，結束時也請按 [停止轉錄]

6.每次使用時請先關閉程式，再重新開啟程式 (否則會累積之前的錄音內容)

7.這是離線的模型，不會連線雲端服務

